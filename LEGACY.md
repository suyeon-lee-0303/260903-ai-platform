# Legacy reference files
#
# - legacy-index.html — original HTML MVP (reference only, do not edit for the React app)
# - Code.gs — original Google Apps Script backend (reference only)
#
# The new app lives at the repo root (Vite + React + TypeScript).
# Use `npm run dev` to start the UI and local API together.
